27. REMOVE ELEMENT

Solution:
class Solution(object):
    def removeElement(self, nums, val):
        k=0
        for i in range(len(nums)):
            if nums[i]!= val:
                nums[k] = nums[i]
                k+=1
        return k

APPROACH- 2pointers, arrays, (!= and replace to form new soln)

